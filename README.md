to use the script create a file on your OpenWRT device in /usr/bin/ called wg-friendly.
copy the scriptcontent into the file.
make it executable with 
 chmod +x wg-friendly
now the script can executed from everywhere with 
 wg-friendly
